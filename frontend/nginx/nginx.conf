user nginx;
worker_processes 2;

http {
  log_not_found off;
  sendfile  on;

  server {
    listen 80;
      location / {
        root   /build;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
      }
}